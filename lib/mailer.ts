/**
 * Nodemailer transport + send function.
 * Implementation deferred until nodemailer is installed.
 */

// TODO: implement SMTP transport using env vars (SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_APP_PASSWORD).
